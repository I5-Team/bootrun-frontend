import styled from 'styled-components';

export const AboutTeamPageStyles = {
  Page: styled.div`
    width: 100%;
    min-height: 100vh;
    background: ${({ theme }) => theme.colors.gray100};
  `,
};

export default AboutTeamPageStyles;
